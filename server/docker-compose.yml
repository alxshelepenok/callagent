version: "3.9"

services:
  server:
    build: .
    ports:
      - "3100:3100"
    volumes:
      - uploads:/app/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  uploads:
